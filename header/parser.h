#include <iostream>
#include <string>
#include <stdio.h>
#include <vector>
//td::string parser(std::string qToParse);
class Parser()
{
    public::
        Parser();
        Parcer(Command*)
        Parcer(Command*, string)

        
}

